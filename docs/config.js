// Configuration for the DocForge Frontend
// When deployed to Vercel, set this to your Render/Cloud Run backend URL.
// If null/empty, it defaults to the same origin (window.location.origin).

window.DOCFORGE_API_URL = ""; // e.g. "https://docforge-api.onrender.com"
